<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Lets the countdown Timer fire reliably via AlarmManager even if the
         app is backgrounded or killed. On API 31-32 this is enough by
         itself; on API 33+ the OS may still require the user to grant it
         via Settings, in which case the app falls back to an inexact
         (but still Doze-bypassing) schedule automatically. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudyFlow">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Foreground Study Service -->
        <service
            android:name=".StudyForegroundService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- Alarm stop receiver (notification action button) -->
        <receiver
            android:name=".AlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.studyflow.app.STOP_ALARM" />
            </intent-filter>
        </receiver>

        <!-- Fires the countdown Timer's completion alarm via AlarmManager,
             independent of whether the app process is alive. -->
        <receiver
            android:name=".TimerAlarmReceiver"
            android:exported="false" />

        <!-- Study Time Widget -->
        <receiver
            android:name=".widget.StudyTimeWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/study_time_widget_info" />
        </receiver>

        <!-- Timer Widget -->
        <receiver
            android:name=".widget.TimerWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/timer_widget_info" />
        </receiver>

    </application>

</manifest>
